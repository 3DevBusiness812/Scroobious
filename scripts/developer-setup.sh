### Note: none of this is required yet

# Install AWS CLI
brew install awscli

# Make sure we're at the latest
brew update

# Make sure everything is installed
which aws

# Set up your AWS credentials
echo "You can find the AWS creds in LastPass under AWS api-user credentials"
aws configure
